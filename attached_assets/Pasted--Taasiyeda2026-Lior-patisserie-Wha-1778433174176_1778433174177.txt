בצע תיקון ממוקד בריפו:
Taasiyeda2026/Lior-patisserie

מטרה:
לתקן את כפתורי החיבור / ההזמנה ב־WhatsApp כך שיעבדו תמיד, גם אם מספר הווטסאפ הוזן בפורמט ישראלי מקומי כמו 050-642-2900, וגם אם הוא הוזן בפורמט בינלאומי כמו 972506422900.

תיקונים נדרשים:

1. תיקון יצירת קישור WhatsApp

בדוק את כל המקומות בקוד שבהם נבנה קישור WhatsApp:
- index.html
- site-content.js
- כל פונקציה או listener שמייצרים href ל־wa.me
- כל כפתור עם data-whatsapp
- כל כפתור עם data-order
- כפתור שליחת הזמנה מתוך המודל / טופס ההזמנה

יש ליצור פונקציה מרכזית אחת לנרמול מספר ל־WhatsApp, למשל:

function normalizeWhatsappPhone(value) {
  const digits = String(value || "").replace(/[^0-9]/g, "");

  if (!digits) return "";

  // פורמט ישראלי מקומי: 0506422900 -> 972506422900
  if (digits.startsWith("0") && digits.length === 10) {
    return `972${digits.slice(1)}`;
  }

  // פורמט ישראלי בינלאומי עם 972
  if (digits.startsWith("972") && digits.length === 12) {
    return digits;
  }

  // אם הוזן 00972...
  if (digits.startsWith("00972")) {
    return digits.replace(/^00/, "");
  }

  return digits;
}

כל קישור WhatsApp חייב להיבנות רק עם הפונקציה הזאת.

דוגמה לתוצאה תקינה:
050-642-2900
צריך להפוך ל:
https://wa.me/972506422900?text=...

אסור שיווצר קישור כזה:
https://wa.me/0506422900

2. תיקון buildWhatsAppUrl

עדכן את buildWhatsAppUrl כך שיקבל מספר בכל פורמט, ינרמל אותו, ורק אז יבנה את הקישור.

מבנה תקין:

function buildWhatsAppUrl(phone, productName = "") {
  const whatsappPhone = normalizeWhatsappPhone(phone);
  if (!whatsappPhone) return "#";

  const productLine = productName ? `שם המוצר: ${productName}` : "שם המוצר:";
  const message = `שלום ליאור, אשמח לבצע הזמנה מ־Lior’s Pâtisserie.\n${productLine}\nכמות:\nתאריך רצוי:\nשם מלא:\nהערות:`;

  return `https://wa.me/${whatsappPhone}?text=${encodeURIComponent(message)}`;
}

3. לוודא שכל כפתורי ההזמנה עובדים

יש לוודא ש:
- כפתור ההזמנה הראשי פותח את טופס ההזמנה או את WhatsApp לפי ההתנהגות הקיימת באתר.
- כפתורי "להזמנה" בכרטיסי המוצרים עובדים גם במוצרים שמגיעים דינמית מ־Supabase.
- כפתור "שליחת הזמנה ב־WhatsApp" בתוך הטופס יוצר קישור תקין ופותח WhatsApp בטאב חדש.
- אם מוצרים נטענים מחדש דינמית, אין להסתמך רק על querySelectorAll חד־פעמי בזמן טעינת העמוד. עדיף להשתמש ב־event delegation על document או להריץ מחדש את חיבורי הכפתורים אחרי renderManagedProducts.

4. ברירת מחדל למספר WhatsApp

אם אין מספר תקין ב־Supabase, השתמש כברירת מחדל במספר:
972506422900

אבל אל תדרוס ערך קיים ב־Supabase. רק fallback בצד הלקוח.

5. תיקון עמוד האדמין — כפתורים קומפקטיים

בקובץ admin.css:
להקטין את כל כפתורי האדמין כך שיהיו קומפקטיים יותר, באותו קנה מידה של שדה הסיסמה וכפתור "כניסה לניהול".

עדכן את:
.ghost-link,
.admin-button

כך שיהיו פחות גדולים:
- min-height: 38px עד 40px
- padding: 8px 16px
- font-size: 0.88rem
- border-radius: 999px
- לשמור על אותו סגנון עיצובי, צבעים, עומק וצל
- לא לשבור את המובייל
- בכפתורים בתוך שורות פעולה, שלא יתפסו רוחב מוגזם אם אין צורך

אם יש כפתורים שנראים גדולים במיוחד כמו:
- שמירת עמוד הבית
- הוספת יתרון
- הוספת מוצר חדש
- שמירת תמונות
- שמירת פרטי הזמנה

להחיל עליהם את אותו גודל קומפקטי.

6. הסרת טקסטים מעמוד האדמין

בקובץ lior-admin.html יש להסיר לגמרי מהתצוגה את הטקסטים הבאים:

להסיר את המילה:
CMS

כלומר לשנות:
Lior’s Pâtisserie CMS
ל:
Lior’s Pâtisserie

להסיר לגמרי את המשפט:
עריכת תוכן האתר הסטטי דרך Supabase — בלי לשנות את עיצוב האתר או את קבצי ה־fallback המקומיים.

להסיר לגמרי את המשפט:
הגנה זמנית בלבד עד להגדרת Supabase Auth אמיתי. אין להשתמש כאן ב־service role key.

להסיר גם את המילה:
זמנית

כלומר אם מופיע:
סיסמת ניהול זמנית
לשנות ל:
סיסמת ניהול

7. לא לשנות דברים לא קשורים

אין לשנות:
- עיצוב האתר הראשי מעבר למה שנדרש לכפתורי WhatsApp
- מבנה Supabase
- שמות טבלאות
- fallback של מוצרים ותמונות
- פונטים
- צבעוניות כללית
- עמודים לא קשורים

8. בדיקות חובה

לאחר התיקון לבדוק ידנית בקוד:

- מספר 050-642-2900 יוצר:
  https://wa.me/972506422900

- מספר 972506422900 נשאר:
  https://wa.me/972506422900

- מספר עם מקפים/רווחים/פלוס:
  +972 50-642-2900
  הופך ל:
  972506422900

- לחיצה על כפתור ההזמנה הראשי לא נתקעת על href="#".
- לחיצה על "שליחת הזמנה ב־WhatsApp" פותחת קישור wa.me תקין.
- אין שגיאות Console.
- עמוד האדמין עדיין נטען, הכניסה לניהול עדיין עובדת, וכל הכפתורים נראים קומפקטיים יותר.